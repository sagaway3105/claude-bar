# トリプルバブル設計（v1.6候補・未実装）

設定でバブルを3つに増やせるモード。3つのメトリクスを同時に浮遊表示し、
バブル同士が近づくと表面張力のようにくっつく。

## 仕様（2026-07-29 ユーザー決定）

- 設定でバブル表示を「1つ / 3つ」から選択できる
- **3つモード中は「表示する使用量」の選択Pickerを無効化**（3つで全メトリクスが見えるため。
  設定値自体は保持し、1つモードに戻したら従来どおり効く）
- 表示内容と序列: **①現在のセッション（主役・最大） ②Fable週間 ③週間すべてのモデル**。
  重要度順に少しずつ小さくする
- 3つはそれぞれふわふわ漂う
- 一塊としてドラッグで動かせる
- 個別にも狭い範囲で動かせる
- **バブル同士が少し重なると表面張力のようにくっつく（最重要の見せ場）**

## 全体構成

### ウィンドウ: 1枚の共有ウィンドウに3アセンブリ

現行の「固定透明ウィンドウ+内部アセンブリのみ浮遊」を拡張し、
**約360×300ptの固定ウィンドウ1枚**に3球を置く。

- 別ウィンドウ×3は不採用: ウィンドウを跨ぐネック（くびれ）描画が不可能・塊ドラッグが複雑化
- 塊ドラッグ=ウィンドウ移動なので、現行のfloatBoundsクランプ・メニューバー吸着をそのまま流用
- シングルモードは現行実装を**無変更**で残す（`bubblePanel`と別に`triplePanel`を持つか、
  サイズ/内容を切り替えるかは実装時判断。退行ゼロを優先）

### サイズ階層

| 球 | 基準径 | 使用量スケール |
|---|---|---|
| 現在のセッション | 76pt | 現行と同じ `bubbleScaleFactor`（10%ごと+2%・最大1.2倍） |
| Fable週間 | 64pt | 同上 |
| 週間すべてのモデル | 54pt | 同上 |

### 配置ルール（2026-07-29 ユーザー決定）

**上からセッション → Fable → 週間**の重要度順。ただし真っ直ぐ縦に並べず、
**左右へ振ったジグザグ**にして塊をなるべく小さくまとめる。

```
   ( セッション )        ← 最上・最大・やや左
        ( Fable )       ← 右へ振る
   ( 週間 )             ← 最下・最小・左へ戻す
```

- 隣同士（セッション⇔Fable、Fable⇔週間）はホーム位置でわずかに重なる距離 = 常にくっついて見える
- 離れた組（セッション⇔週間）はネックを張らない距離にして、団子にならないようにする
- 個別に動かしても**ゆるいばねでホームへ戻る**ため、自然とこの優先度配置へ落ち着く。
  ばねは弱め（戻り切らずに漂い続ける程度）で、**あくまで「なんとなくその並び」**に見えればよい
- 実測に使ったホーム座標（半径38/32/27、360×300ウィンドウ内）: (150,104) / (194,156) / (154,200)

## Phase 0 実測結果（2026-07-29）

単独ベンチプロセス（`swiftc -O`・360×300pt・3球・自プロセスのCPU時間をgetrusageで直接測定）。
ClaudeBar本体の常時活動を含まないクリーンな値。

| 構成 | fps | CPU | 備考 |
|---|---|---|---|
| 3円の塗りのみ | 60 | 3.7% | 基準値 |
| **メタボールPath（塗りのみ）** | 60 | **3.1%** | **幾何計算は激安**（円3つと同等） |
| + union輪郭ストローク | 60 | 5.8% | +2.1% |
| **現行の単一バブル相当**（CAAnimation駆動） | – | **4.2%** | **比較の基準** |
| 3球を個別ガラス（CAAnimation・融合なし） | – | 7.0% | |
| **純正 GlassEffectContainer**（TimelineView駆動） | 60 | **10.3%** | **融合を目視確認済み・美しい** |
| 自前メタボール + `glassEffect(.clear, in:)` | 61 | **24.9%** | ❌ 高すぎる |
| 同上を30fps駆動 | 31 | 15.0% | ❌ |
| 自前メタボール + `.ultraThinMaterial` | 61 | 19.2% | ❌ |

### 結論

1. **ボトルネックは幾何計算ではなく「毎フレーム変わる形状に対する背景ブラー/ガラス描画」**。
   Path計算は3.1%と激安な一方、そこにマテリアル/Liquid Glassを乗せると20%超に跳ね上がる
2. **macOS 26では Apple純正の `GlassEffectContainer(spacing:)` が最適解**。固定形状(Circle)の
   `glassEffect` を入れて位置だけ動かすと、Appleのレンダラが近接した球を自動融合する。
   自前メタボール実装（24.9%）の半分以下（10.3%）で、見た目も有機的で美しい（実機で目視確認済み）
3. 設計上の原則: **バックドロップを参照する層（マテリアル/ガラス）に毎フレーム変わる形状を
   渡してはいけない**。Appleエンジニアの言及どおり、マスク/可変形状はオフスクリーンパスを
   強制し背景キャプチャを壊す

### 未確定（画面ロック解除後に再測定が必要）

- SwiftUIの宣言的アニメーション（`repeatForever`）で球を動かす版が6%前後という値を得たが、
  **測定中に画面がロックされた可能性があり信頼できない**。再測定必須
- 同版で融合が動的に更新されるか（レンダーサーバに委譲された場合、融合が追従しない懸念）の目視確認
- macOS 14/15用のCanvas方式（`alphaThreshold`+blurで静的ガラス板をマスク）の実測。
  リサーチ予測は6%前後。ベンチには実装済み（`canvasMask` / `canvasShape`）

## 表面張力（メタボール）

**Phase 0を踏まえた方式の決定（OS別の二本立て）**

| OS | 方式 | 根拠 |
|---|---|---|
| macOS 26+ | **`GlassEffectContainer(spacing:)` に固定CircleのglassEffectを入れ、位置だけ動かす** | Appleのレンダラが融合を担当。10.3%・見た目最良 |
| macOS 14/15 | **固定バックドロップ + 静的ガラス板を メタボール形状で `.mask`** | バックドロップの再サンプルを避ける唯一の道 |

いずれも「バックドロップ層は固定・変わるのはマスク/位置だけ」という原則を守る。
自前メタボールPath（`MetaballShape.swift`・実装済み）は旧OS側のマスク供給と、
虹色リムの輪郭計算に使う（両方とも背景を参照しないので安い）。

### 描画: 解析的2円コネクタ

2球の縁の距離に応じた「ネック（くびれ）」を解析的にPath計算する（古典的メタボール手法）:

1. 2円の中心距離 d、半径 r1, r2 から左右の接点4つを求める
2. 接点間を2次ベジェで結ぶ。制御点は2円の中間点方向へ、`spread`（d が近いほど太い）で張る
3. ネックの発生条件: `d < r1 + r2 + 吸着マージン`。d が縮むほどくびれが太くなり、
   完全に重なると円の合併輪郭に漸近

3球分は各ペア（最大3ペア）で独立に計算し、円3つ+ネックPathをunionした
**1つの結合シルエットShape**を作る。

### Shapeの3役使い回し（OS互換の要）

| 用途 | macOS 26 | 旧OS（14+ / CLAUDEBAR_FORCE_LEGACY） |
|---|---|---|
| ガラス | `glassEffect(.clear, in: 結合Shape)` | 結合Shapeを `.ultraThinMaterial` で塗る |
| 虹色リム | 結合Shapeをstroke（融合輪郭に沿って光る） | 同left |
| ヘイズ/下地 | 結合Shape内に描画 | 同左 |

- 26専用の `GlassEffectContainer` 自動融合には**依存しない**（挙動をOS間で揃え、制御可能にする）
- ハイライト・コースティクス・照りは**各球ごと**に残す（球体感の維持）。融合するのは輪郭系のみ

### 吸着/分離の物理（ヒステリシス）

- **吸着**: 縁の距離 < 4pt → 少し重なる位置（重なり6-8pt）へぷにっと引き込み+ハプティクス。
  ネックが伸びる
- **分離**: 縁の距離 > 14pt（引き離し中）→ プルンと震えて離れる（ポヨンの流用）
- 吸着中の漂いは「くっついたまま揺れる」= 2球の相対距離を保ちつつ塊で漂う

## アニメーション方式（現行からの変更点・最大のトレードオフ）

ネックは2球の**現在位置から毎フレーム再計算**が必要なため、現行の
「CAAnimationレンダーサーバ常駐」では描けない。トリプルモードに限り:

- **TimelineView(.animation) 内で、球ごとに周期・位相の違うサイン波合成（4本）を
  決定論的に毎フレーム計算**して各球のオフセットとネックを描く
  （バブル内容は既に30fpsのTimelineViewで動いており構造は同じ。ProMotionで120Hz駆動）
- 個別ドラッグ・吸着スナップ・リーシュはこの座標系上のばね補間で行う
- リスク: メインスレッド詰まり時のカクつき（CAAnimation方式の利点を放棄）。
  **Phase 0のプロトタイプで必ず実測**し、許容外ならハイブリッド
  （漂いはCAAnimation+ネックのみpresentation()追従の軽量オーバーレイ）へ切り替える
- シングルモードは現行CAAnimation方式のまま

## 操作系（修飾キーなし）

| 操作 | 挙動 |
|---|---|
| 球を短くドラッグ | **その球だけ**リーシュ圏内（ホームから半径約28pt）で個別移動 |
| リーシュが張り詰めるまで引く | 風船の束のように**塊全体がついてくる**（=一塊ドラッグ） |
| クリック | その球だけポヨン |
| 3連打 | **その球だけ**破裂（他は残る）。リセット復活も球ごと（reviveBubble設定に従う） |
| 100%到達 | その球だけ破裂 |
| 塊をメニューバー付近で放す | 塊ごと吸着して消える（現行流用） |
| ホバー | HUD表示は維持（リセット時刻が見える価値が残る。塊の右横・漂い同期） |
| 右クリック | 現行メニュー（パネルに展開/閉じる/設定/終了） |

- リセット破裂→再生成（popForReset）は球ごとに独立スケジュール
- 通知(80/95%)・メニューバー%表示はモードと無関係（現行のまま）

## 設定UI

```
バブル
  表示モード:  ◯ 1つ   ◯ 3つ（セッション + Fable + 週間）
  表示する使用量: [現在のセッション ▼]   ← 3つモード中は .disabled(true)
  ☑ 割れた後、リセット時に復活
```

- `SettingsStore` に `bubbleCount`（1 or 3、既定1）を追加。`bubbleMetric` は保持
- シングル用の状態（`bubbleActive`等）とトリプル用の区別、DebugBridgeへの
  `bubble3` トグルコマンド・3球座標のstateダンプ追加（UI検証用）

## 実装フェーズ

| Phase | 内容 | 目安 |
|---|---|---|
| 0 | **リスク潰しプロトタイプ**: ネック描画+glassEffectの毎フレーム更新のGPU/CPU実測、TimelineView駆動の滑らかさ確認（60fps必須要件） | 0.5日 |
| 1 | モード設定+1窓3球+サイズ階層+塊/リーシュドラッグ | 1日 |
| 2 | メタボールネック+吸着/分離ヒステリシス+ぷるん演出 | 1日 |
| 3 | 球ごとの破裂/復活・吸着/HUD統合・旧OS互換（FORCE_LEGACY）・性能検証 | 0.5-1日 |

## 未決事項（実装時に判断）

- 追加利用(extra usage)が有効な場合に4球目を出すか（現時点では出さない=HUD任せ）
- 3球モードでの初回出現アニメーション（順番にぽわんっと3連続が有力）
- ウィンドウ360×300ptで画面端の可動域クランプがきつくないか（実機確認）
